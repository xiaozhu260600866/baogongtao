<!-- <dx-title name="标题" tabs="标签标签标签标签" Lline></dx-title> -->

<template>
	<view class="dxi-header" :class="{myclass}" :style="{background:bgColor,height:height*2+'rpx',}">
		<view class="h-left" :style="{width:leftWidth*2+'rpx',height:height*2+'rpx',lineHeight:height*2+'rpx'}"><dx-icon :name="iconName" :size="iconSize" :color="iconColor" v-if="icon"></dx-icon></view>
		<view class="h-con" :style="{fontSize:nameSize*2+'rpx',color:nameColor,textAlign:nameAlign,fontWeight:bold,padding:`0 ${rightWidth*2}rpx 0  ${leftWidth*2}rpx`}">{{name}}</view>
		<view class="h-right" :style="{width:rightWidth*2+'rpx',height:height*2+'rpx',}"><slot name="right"></slot></view>
	</view>
</template>

<script>
	import dxIcon from "../icon/icon"
	export default{
		components: {
			dxIcon
		},
		props: {
			// 自定义类名
			myclass:{
				type: String, 
				default: ''
			},
			//是否有箭头
			bgColor: {
				type: String,
				default: '#5677fc'
			},
			height:{
				type: Number,
				default: 50,
			},
			leftWidth:{
				type:Number,
				default:50
			},
			rightWidth:{
				type:Number,
				default:50
			},
			// 名称
			name: {
				type: String,
				default: ''
			},
			// 字体大小
			nameSize: {
				type:  Number,
				default: 20
			},
			// 字体颜色
			nameColor: {
				type: String,
				default: '#fff'
			},
			// 字体对齐
			nameAlign: {
				type: String,
				default: 'center'
			},
			// 字体是否加粗
			bold: {
				type: String,
				default: 'normal'
			},
			// 标签
			icon:{
				type:Boolean,
				default: true
			},
			iconName:{
				type:String,
				default: ''
			},
			// 标签字体大小
			iconSize: {
				type:  Number,
				default: 18
			},
			// 标签颜色
			iconColor: {
				type:  String,
				default: '#999'
			},
			// 右插槽
			right: {
				type: String,
				default: ''
			}
			
		},
		data(){
			return {
				
			}
		},
		methods:{
			
		}
	}
</script>
<style>
.dxi-header {position: fixed;width: 100%;box-sizing: border-box;overflow: hidden;display: flex;align-items: center;}
.dxi-header .h-left{position: absolute;left: 0;top: 0;text-align: center;}
.dxi-header .h-left .dxi-icon{display: flex;justify-content: center;}
.dxi-header .h-right{position: absolute;right: 0;top: 0;text-align: center;display: flex;justify-content: center;align-items: center;}
.dxi-header .h-con{width: 100%;flex: 1;overflow: hidden;}
</style>