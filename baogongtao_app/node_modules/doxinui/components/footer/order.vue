<template>
	<view class="dxi-footer-order" :class="[fixed?'footer-fixed':'',myclass]" :style="{padding:padding,background:bgColor,}">
		<view class="left"><slot name="left"></slot></view>
		<view class="right">
			<dx-button :type="[item.cur?'danger':'']" :size="[fixed?'medium':'small']" round v-for="item in data" v-if="item.show" @tap="$emit('click')">{{ item.name }}</dx-button>
		</view>
	</view>
</template>
<script>
	import dxButton from "../button/button.vue"
	export default{
		props: {
			// 自定义类名
			myclass:{
				type: String, 
				default: ''
			},
			fixed:{
				type:Boolean,
				default: false
			},
			padding:{
				type:String,
				default: '0 10rpx'
			},
			bgColor:{
				type:String,
				default: '#fff'
			},
			data: {
				type: Array,
				default () {
					return []
				}
			}
		},
		components: {
			dxButton
		},
		data(){
			return {
				
			}
		},
		methods:{
		}
	}
</script>
<style>
.dxi-footer-order{display: flex;justify-content: space-between;align-items: center;min-height: 84upx;}
.dxi-footer-order.footer-fixed{position: fixed;bottom: 0;left: 0;border-top: 1upx #f5f5f5 solid;width: 100%;min-height: 100upx;}
.dxi-footer-order /deep/.dxi-btn{margin: 0 10upx;}
.dxi-footer-order .right{flex: 1;text-align: right;}
</style>