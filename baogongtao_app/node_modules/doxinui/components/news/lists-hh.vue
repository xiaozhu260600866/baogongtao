<template>
	<view class="dxi-news-hc" :class="[myclass]">
		<block v-for="(item,index) in data" :key="index">
			<view class="dxi-news-hc-row dxi-news-hc-flex" :class="[item.time||item.imgNum>1?'dxi-flex-column':'dxi-flex-start',noBorder?'noBorder':'']"
			 :style="{padding:`${tbPadding*2}rpx ${lrPadding*2}rpx`}">
				<view class="dxi-news-hc-pic">
					<block v-for="(items,index2) in item.img" :key="index2">
						<image :src="items" mode="aspectFill" class="img" :class="[item.imgNum>1?'dxi-one-third':'',item.time?'pic-full':'']"></image>
					</block>
					<view class="pic-badge" v-if="item.time||item.imgNum>3">{{item.time?item.time:item.imgNum+'图'}}</view>
					<view class="dxi-video" v-if="item.time">
						<dx-icon name="play" size="16" color="#f5f5f5"></dx-icon>
					</view>
				</view>
				<view class="dxi-news-hc-box dxi-flex-column dxi-flex-between" :class="[item.imgNum===1 && !item.time?'boxH-82 pl12':'']">
					<view class="dxi-news-hc-title nowrap" :style="{fontSize:titleSize*2+'rpx',color:titleColor}"
					 :class="[item.time || item.imgNum>1 ?'pt12': '']">{{item.title}}</view>
					<view class="dxi-news-hc-remark wrap2" :style="{fontSize:remarkSize*2+'rpx',color:remarkColor}" v-if="item.remark"
					 :class="[item.imgNum==1 && !item.time?'':'pt8']">{{item.remark}}</view>
				</view>
			</view>
		</block>
	</view>
</template>

<script>
	import dxIcon from "../icon/icon"
	export default{
		components: {
			dxIcon
		},
		props: {
			// 自定义类名
			myclass:{
				type: String, 
				default: ''
			},
			// 数组
			data: {
				type: Array,
				default () {
					return []
				}
			},
			// 整框左右距离
			lrPadding:{
				type:Number,
				default: 15
			},
			// 整框上下距离
			tbPadding:{
				type:Number,
				default: 12
			},
			titleSize:{
				type:Number,
				default: 17
			},
			titleColor:{
				type:String,
				default: '#333'
			},
			remarkSize:{
				type:Number,
				default: 14
			},
			remarkColor:{
				type:String,
				default: '#777'
			},
			noBorder:{
				type:Boolean,
				default: false
			}
		},
		data(){
			return {
				
			}
		},
		methods:{
			
		}
	}
</script>
<style>
.dxi-news-hc-flex {width: 100%;display: flex;background-color: #fff;}
.dxi-flex-start {align-items: flex-start !important;}
.dxi-flex-center {align-items: center !important;}
.dxi-flex-column {flex-direction: column !important;}
.dxi-flex-between {justify-content: space-between !important;}

.dxi-news-hc-row{border-bottom: 1upx #eee solid;}

.dxi-news-hc-pic {display: flex;position: relative;justify-content: space-between;}
.dxi-news-hc-pic .img{width: 220upx;height: 164upx;border-radius: 12upx;}
.dxi-news-hc-pic .dxi-one-third{width: 33%;border-radius: 0;}
.dxi-news-hc-pic .pic-full{width: 100%;display: block;height: 276upx;}

.dxi-news-hc-box{flex: 1;width: 100%;overflow: hidden;display: flex;}
.dxi-news-hc-box.boxH-82{min-height: 164upx;}
.dxi-news-hc-remark{line-height: 1.4;}

.pic-badge {position: absolute;right: 0;bottom: 0;font-size: 24rpx;color: #fff;padding: 2rpx 12rpx;background: rgba(0, 0, 0, 0.6);z-index: 20;transform: scale(0.8);transform-origin: 100% 100%;}
.dxi-video {position: absolute;z-index: 10;display: flex;align-items: center;justify-content: center;left: 50%;top: 50%;transform: translate(-50%, -50%);transform-origin: 0 0;}
.dxi-icon {background: rgba(0, 0, 0, 0.5);border-radius: 50%;padding: 26rpx;}
.dxi-icon-box .dx-icon {background: none;padding: 0;border-radius: 0;}


</style>